import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'l03-assignment',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
